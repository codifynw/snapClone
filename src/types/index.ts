export type Snap = {
  duration: number;
  imgUrl: string;
  from: number;
  timestamp: number;
  id: number;
};

export type ImgOverlay = {
  duration: number
  imgUrl: string
}

export type User = {
  id: number
  name: string
}